
# C build artifacts
build/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Julia package (AlmostIdempotentChannels.jl, bead aic-m24) — deps.jl and
# build.log are regenerated by Pkg.build (deps/build.jl). Project.toml and the
# resolved Manifest.toml ARE committed (pinned versions = reproducible SDP values).
julia/AlmostIdempotentChannels.jl/deps/deps.jl
julia/AlmostIdempotentChannels.jl/deps/build.log

# agent worktrees (transient)
.claude/worktrees/

# docs plotting env — regeneratable; Manifest pins an absolute dev path
docs/plots/Manifest.toml

# Documenter generated site + docs Manifest (regeneratable)
docs/build/
docs/Manifest.toml

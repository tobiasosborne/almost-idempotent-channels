# API reference

Every exported name of `AlmostIdempotentChannels`, rendered from its docstring.
The page is generated by `@autodocs` over the whole module, so a docstring added
to an export shows up here automatically.

## The headline verbs

These read like the paper — nouns are the math objects, verbs are the
constructions:

```julia
Φ = UCPMap(kraus)              # the UCP self-map Φ: B(H) → B(H)
η = certified_defect(Φ)        # rigorous bracket on ‖Φ²−Φ‖_cb, NO solver (default)
A = associated_algebra(Φ)      # the ε-C* algebra A = Img Φ̃  (ε = O(η))
v = main_isomorphism(Φ)        # O(ε)-isomorphism v: B → A, dim-independent constant
F = factorize(Φ)               # Φ ≈ Δ Υ through a genuine C* algebra B = ⊕_l M_{d_l}
```

## Index

```@index
Modules = [AlmostIdempotentChannels]
```

## Docstrings

```@autodocs
Modules = [AlmostIdempotentChannels]
```
